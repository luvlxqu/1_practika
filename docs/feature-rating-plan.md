# План реализации: Рейтинг и отзывы

## API Endpoints
- POST /reviews/create — создание
- GET /reviews/specialist/<id> — список
- GET /reviews/average/<id> — рейтинг
- POST /reviews/approve/<id> — модерация

## Компоненты
- StarRating.vue — звёзды
- ReviewForm.vue — форма
- ReviewList.vue — список
